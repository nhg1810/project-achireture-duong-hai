{{!-- js for this page --}}
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>
{{!-- sweet alert --}}
<script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<script>
    $(document).ready(function () {
        $(".btn-login-admin").click(function () {
            let userName = $(".username").val();
            let passWord = $(".password").val();
            let formData = new FormData();
            if (userName == "" || passWord == "") {
                $(".alert-error").html('Chưa điền đủ tên đăng nhập hoặc mật khẩu !');
            } else {
                formData = {
                    userName: userName,
                    passWord: passWord
                }
                $.ajax({
                    url: '/admin',
                    type: 'POST',
                    data: formData,
                    dataType: "json",
                    encode: true,
                }).done(function (data) {
                    console.log(data);
                    if (data.status == 'error') {
                        $(".alert-error").html('Sai taì khoản mật khẩu ! Vui lòng kiểm tra lại !')
                    } else if (data.status == 'success') {
                        setCookie('token', data.data.token, 1);
                        window.location.href = "/admin";
                    }
                })
            }
        })
        function setCookie(cname, cvalue, exdays) {
            const d = new Date();
            d.setTime(d.getTime() + (exdays * 24 * 60 * 60 * 1000));
            let expires = "expires=" + d.toUTCString();
            document.cookie = cname + "=" + cvalue + ";" + expires + ";path=/";
        }
    })
</script>

<div class="content-wrapper d-flex align-items-center auth px-0">
    <div class="row w-100 mx-0">
        <div class="col-lg-4 mx-auto">
            <div class="auth-form-light text-left py-5 px-4 px-sm-5">
                <div class="brand-logo">
                    <img src="../../images/logo.svg" alt="logo">
                </div>
                <h4>Xin chào! </h4>
                <h6 class="fw-light">Đăng nhập để tiếp tục.</h6>
                <p class="alert-error" style="color: red;"></p>
                <form class="pt-3">
                    <div class="form-group">
                        <input type="usename" class="form-control form-control-lg username" id="exampleInputEmail1"
                            placeholder="Tên đăng nhập">
                    </div>
                    <div class="form-group">
                        <input type="password" class="form-control form-control-lg password" id="exampleInputPassword1"
                            placeholder="Mật khẩu">
                    </div>
                    <div class="mt-3 btn-login-admin">
                        <a class="btn btn-block btn-primary btn-lg font-weight-medium auth-form-btn"
                            href="javascript: value(0)">Đăng nhập</a>
                    </div>
                    <div class="my-2 d-flex justify-content-between align-items-center">
                        <div class="form-check">
                            <label class="form-check-label text-muted">
                                <input type="checkbox" class="form-check-input">
                                Giữ luôn đăng nhập
                            </label>
                        </div>
                        <a href="#" class="auth-link text-black">Quên mật khẩu ?</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>